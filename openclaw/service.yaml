apiVersion: v1
kind: Service
metadata:
  name: openclaw-service
  labels:
    app: openclaw
spec:
  selector:
    app: openclaw-gateway
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
  type: ClusterIP
